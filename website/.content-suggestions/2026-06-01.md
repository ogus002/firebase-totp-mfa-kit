# Content suggestion — 2026-06-01 (UTC)

Generated by `website/scripts/generate-content.ts` against `pages/firebase-totp-mfa-setup.tsx`.

Review, apply manually if useful, or close the PR if not.

---

## Identified Issue

The "Install" section states the CLI copies files into `app/(auth)/mfa-enroll`, which is an **App Router** path. However, the page's own `<p>` intro claims it "Works on Next.js App Router and Pages Router," and the page itself is Pages Router TSX. A Pages Router user would receive files at a different path (e.g. `pages/mfa-enroll`), so the hardcoded App Router path is misleading and leaves Pages Router users without the information they need to find their generated files.

The fix adds a one-line clarification noting the actual output path for each router so users are not confused when they cannot find `app/(auth)/mfa-enroll` in a Pages Router project.

```diff
--- a/pages/setup.tsx
+++ b/pages/setup.tsx
@@ -19,8 +19,10 @@ export default function Setup() {
       <h2 className="text-2xl font-semibold mt-8 mb-3">1. Install</h2>
       <p className="mb-3">
         Run the CLI in your Next.js project. It detects your framework, copies
-        the source files into <code>app/(auth)/mfa-enroll</code>, and wires the
-        route into your existing layout.
+        the source files into <code>app/(auth)/mfa-enroll</code> (App Router)
+        or <code>pages/mfa-enroll</code> (Pages Router), and wires the route
+        into your existing layout.
       </p>
       <pre className="bg-slate-900 text-slate-100 p-4 rounded text-sm overflow-x-auto">
         npx firebase-totp-mfa add next --area /admin --issuer "MyApp"
       </pre>
```
